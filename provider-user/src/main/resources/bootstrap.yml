spring:
  application:
    name: provider-user
  cloud:
    config:
      enabled: true
      uri: http://localhost:8762/
      profile: local
  rabbitmq:
    addresses: localhost:5672
    username: guest
    password: guest
  profiles:
    active: local
