server:
  port: 8081

spring:
  security:
    user:
      name: bone
      password: 123456
  cloud:
    config:
      server:
        svn:
          uri: "http://192.168.200.52/svn200/k9s-9/branches"
          searchPaths: "/global/{profile}/,/{application}/{profile}"
          defaultLabel: "k9s-9_187_142_819_201903081245"
          username: "yxl"
          password: "a111111"

eureka:
  client:
    serviceUrl:
      defaultZone: ${BONE_EUREKA_SERVER_URL:http://${spring.security.user.name}:${spring.security.user.password}@localhost:8080/eureka/}

# 日志
logging:
  level:
    root: INFO
